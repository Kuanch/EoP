version: "3"
services:
  ntfy:
    image: binwiederhier/ntfy
    container_name: eop-ntfy
    command: serve
    ports:
      - "8090:80"
    volumes:
      - ./data/ntfy:/var/cache/ntfy
    restart: unless-stopped
